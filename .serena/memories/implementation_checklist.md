# Unlooped MVP - 구현 체크리스트

**문서 최종 수정**: 2025-12-01  
**기준 문서**: docs/prd_v2.md

---

## Phase 1 - MVP (현재 구현 목표)

### 1. 프로젝트 기반 인프라

#### 1.1 기술 스택 설정
- [x] Next.js 14 설정 (App Router)
- [x] TypeScript 설정
- [x] Tailwind CSS 4 설정
- [x] Radix UI 설치
- [x] Lucide React 아이콘
- [x] TanStack React Query v5 설정
- [x] Supabase 클라이언트 설정
- [x] Prettier & ESLint 설정

#### 1.2 Supabase 설정
- [x] Supabase 프로젝트 생성
- [x] 환경 변수 설정 (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY)
- [x] 클라이언트/서버 Supabase 클라이언트 분리 (app/lib/supabase/client.ts, server.ts)
- [x] TypeScript 타입 생성 (types/supabase.ts)

#### 1.3 프로바이더 설정
- [x] QueryProvider 구현 (app/providers/QueryProvider.tsx)
- [x] AuthProvider 구현 (app/providers/AuthProvider.tsx)
- [x] Layout에 Provider 적용 (app/layout.tsx)

---

### 2. 데이터베이스 설계

#### 2.1 테이블 생성
- [x] users 테이블 (id, email, username, avatar_url)
- [x] memo 테이블 (id, user_id, content, created_at, updated_at)
- [❌] **entity 테이블 - user_id 컬럼 누락!** 
  - 현재: id, name, type, description, summary, start_date
  - 필요: **user_id 추가 필요** (RLS 정책 적용 위해)
- [x] memo_entity 테이블 (memo_id, entity_id, created_at)
- [~] entity_relation 테이블 (Phase 3 기능, 현재는 불필요)

#### 2.2 Row Level Security (RLS) 정책
- [❌] users 테이블 RLS 정책 (본인 데이터만 조회)
- [❌] memo 테이블 RLS 정책 (본인 메모만 CRUD)
- [❌] **entity 테이블 RLS 정책 (user_id 컬럼 필요)** ⚠️ 우선순위 높음
- [❌] memo_entity 테이블 RLS 정책

#### 2.3 인덱스
- [❌] memo.user_id 인덱스
- [❌] entity.user_id 인덱스
- [❌] entity.name 인덱스
- [❌] memo_entity.memo_id 인덱스
- [❌] memo_entity.entity_id 인덱스

---

### 3. 사용자 인증 (User Auth)

#### 3.1 인증 로직
- [x] AuthProvider 구현
  - [x] Google OAuth 로그인
  - [x] Github OAuth 로그인
  - [x] Email/Password 로그인
  - [x] Email/Password 회원가입
  - [x] 로그아웃 (Server Action)
  - [x] 인증 상태 관리 (user, session, userProfile)
  - [x] users 테이블 자동 생성 (auth.users → public.users)
  - [x] 소셜 로그인 아바타 자동 동기화

#### 3.2 UI 컴포넌트
- [x] LoginModal 구현 (app/components/auth/LoginModal.tsx)
  - [x] Google/Github 소셜 로그인 버튼
  - [x] Email/Password 폼
  - [x] 회원가입/로그인 전환
  - [x] 에러 표시
  - [x] 로딩 상태
  - [x] 미인증 시 모달 닫기 방지

#### 3.3 인증 상태 체크
- [x] 로그인 상태 전역 관리 (AuthContext)
- [❌] 미인증 사용자 리다이렉트 (현재 모달만 표시)
- [❌] URL 변경 시마다 인증 체크 (Middleware 또는 layout 레벨)

---

### 4. React Query 훅

#### 4.1 Generic 훅 구현 (app/lib/queries.ts)
- [x] useUser() - 현재 인증 사용자 조회
- [x] useTableData<T>() - 테이블 데이터 조회
- [x] useInsertData<T>() - 데이터 생성 + 캐시 무효화
- [x] useUpdateData<T>() - 데이터 수정 + 캐시 무효화
- [x] useDeleteData<T>() - 데이터 삭제 + 캐시 무효화

#### 4.2 도메인 특화 훅 (TODO)
- [❌] useMemos() - 사용자별 메모 조회 (created_at DESC)
- [❌] useCreateMemo() - 메모 생성 (memo + memo_entity 관계)
- [❌] useEntities() - 사용자별 Entity 전체 조회
- [❌] useCreateEntity() - Entity 생성
- [❌] useMemosByEntity(entityId) - Entity별 메모 필터링

---

### 5. 메모 작성 기능 (Memo Creation)

#### 5.1 Input 창 (app/components/InputArea.tsx)
- [x] contentEditable div 기본 구조
- [x] placeholder 표시 (CSS)
- [x] 포맷팅 버튼 (Bold, Italic, List) - UI만
- [❌] **메모 저장 기능 연결** ⚠️ 우선순위 최고
  - 현재: console.log만
  - 필요: useCreateMemo() 훅 연결
- [❌] Enter 키 저장 (Shift+Enter는 줄바꿈)
- [❌] 저장 후 Input 창 자동 초기화
- [❌] 저장 중 로딩 상태 (버튼 disabled)
- [❌] @ 입력 감지
- [❌] Entity 자동완성 드롭다운 (dropup) 표시

#### 5.2 Entity 자동완성 시스템
- [❌] **Entity 드롭다운 컴포넌트 생성** ⚠️ 우선순위 최고
  - [❌] @ 입력 시 드롭다운 표시
  - [❌] 위치: Input 창 위 (dropup)
  - [❌] 캐시된 Entity 리스트 필터링 (startsWith)
  - [❌] 키보드 네비게이션 (방향키, Enter, Esc)
  - [❌] 마우스 클릭 선택
  - [❌] 스페이스바로 Entity 확정
- [❌] 새 Entity 생성 로직
  - [❌] 이름 유효성 검사 (/^[가-힣a-zA-Z0-9]{1,20}$/)
  - [❌] 중복 체크
  - [❌] DB에 즉시 생성
  - [❌] 캐시 업데이트
  - [❌] Toast 피드백
- [❌] 다중 Entity 입력 지원

#### 5.3 메모 저장 프로세스
- [❌] memo 테이블에 레코드 삽입
- [❌] 정규표현식으로 @entity_name 패턴 추출 (/@([가-힣a-zA-Z0-9]+)/g)
- [❌] 각 entity에 대해 memo_entity 관계 생성
- [❌] Optimistic Update (즉시 UI 반영)
- [❌] 에러 처리 (Toast)

---

### 6. 히스토리 섹션 (History Sidebar)

#### 6.1 메모 목록 표시 (app/components/MainContainer.tsx)
- [x] 기본 레이아웃 구조
- [x] 스크롤 영역 설정
- [❌] **메모 데이터 fetch** ⚠️ 우선순위 최고
  - 현재: placeholder만
  - 필요: useMemos() 훅 연결
- [❌] 메모 카드 컴포넌트 생성
  - [❌] 작성 시간 표시 (created_at)
  - [❌] 메모 내용 표시 (content)
  - [❌] Entity 하이라이트 (@entity_name → 색상 배경)
- [❌] 정렬: created_at DESC (최신순)
- [❌] 맨 아래로 자동 스크롤 (useEffect)
- [❌] 로딩 상태 (스켈레톤 UI)
- [❌] 빈 상태 메시지 (메모 없을 때)

#### 6.2 Entity 하이라이트
- [❌] 정규표현식으로 @entity_name 패턴 찾기
- [❌] React 컴포넌트로 치환
- [❌] 배경색 적용 (color-mention-project, person, event)
- [❌] Entity별 색상 매핑 (type에 따라)

#### 6.3 우측 사이드바 (app/components/RightSidebar.tsx)
- [x] 기본 레이아웃 구조
- [x] 스크롤 영역 설정
- [❌] 히스토리 데이터 표시 (MainContainer와 동일 또는 날짜 그룹화)
- [❌] 맨 아래로 자동 스크롤

---

### 7. 전역 에러 처리 및 피드백

#### 7.1 Toast 시스템
- [❌] **Toast Provider 설정** ⚠️ 우선순위 높음
  - 추천 라이브러리: Radix UI Toast 또는 Sonner
- [❌] Toast 컴포넌트 구현
- [❌] 에러 Toast (빨강, 5초)
- [❌] 성공 Toast (초록, 3초)
- [❌] 정보 Toast (파랑, 3초)

#### 7.2 에러 메시지 매핑
- [❌] Supabase 에러 코드별 메시지
  - [❌] 네트워크 오류: "네트워크 연결을 확인해주세요."
  - [❌] 인증 오류: "로그인이 필요합니다."
  - [❌] 권한 오류: "접근 권한이 없습니다."
  - [❌] 메모 저장 실패: "메모 저장에 실패했습니다."
  - [❌] Entity 생성 실패: "엔티티 생성에 실패했습니다."

#### 7.3 성공 피드백
- [❌] 메모 저장: "메모가 저장되었습니다." (선택적)
- [❌] Entity 생성: "✨ 새 엔티티 '{name}'이(가) 생성되었습니다"

---

### 8. UI 컴포넌트

#### 8.1 Header (app/components/Header.tsx)
- [x] 로고 및 타이틀
- [x] 검색 바 (UI만, 기능 미구현)
- [x] 네비게이션 링크 (UI만)
- [x] 알림 아이콘 + Popover (Radix UI)
- [x] 설정 아이콘 + Tooltip (Radix UI)
- [x] 프로필 아바타 + DropdownMenu (Radix UI)
  - [x] 사용자 정보 표시
  - [x] 로그아웃 버튼 (동작 완료)

#### 8.2 메인 레이아웃 (app/page.tsx)
- [x] 전체 레이아웃 구조 (70% - 30%)
- [x] Header 포함
- [x] MainContainer + InputArea (좌측)
- [x] RightSidebar (우측)

---

### 9. 스타일링

#### 9.1 디자인 시스템 (app/globals.css)
- [x] 색상 팔레트 정의
  - [x] bg-primary, bg-secondary, bg-card, bg-input
  - [x] border-main, text-muted
  - [x] mention-project, mention-person, mention-event
- [x] 커스텀 스크롤바 스타일
- [x] contentEditable placeholder 스타일

---

## Phase 1 완료 기준

다음 기능들이 모두 작동해야 Phase 1 완료:
1. ✅ Google/Github OAuth 로그인
2. ❌ 메모 작성 (@ 없이 일반 텍스트)
3. ❌ @ 입력 시 Entity 자동완성
4. ❌ 새 Entity 생성
5. ❌ 히스토리에 메모 표시 (Entity 하이라이트)
6. ❌ Toast 피드백

---

## Phase 2 - 사용성 개선 (TODO)

### 우선순위 1 (높음)
- [ ] 실시간 히스토리 필터링 (@ 입력 중)
- [ ] 히스토리 페이지네이션 또는 무한 스크롤
- [ ] 메모 클릭 시 상세 보기 모달
- [ ] Entity 클릭 시 필터링 기능

### 우선순위 2 (중간)
- [ ] 메모 수정 기능
- [ ] 메모 삭제 기능
- [ ] Entity 이름 수정
- [ ] Entity 삭제

### 우선순위 3 (낮음)
- [ ] 반응형 디자인 (모바일/태블릿)
- [ ] Entity 색상/아이콘 커스터마이징
- [ ] 메모 검색 기능
- [ ] 메모 내보내기 (export)

---

## Phase 3 - 고급 기능 (TODO)

- [ ] 메모 간 연결 (backlink)
- [ ] 그래프 뷰 (entity 관계도)
- [ ] AI 기반 메모 추천
- [ ] 협업 기능 (공유 entity)

---

## 🚨 긴급 수정 필요 사항 (Phase 1 블로커)

### 1. 데이터베이스 스키마 수정
**문제**: entity 테이블에 user_id 컬럼이 없어 RLS 정책을 적용할 수 없음

**해결 방법**:
```sql
-- entity 테이블에 user_id 추가
ALTER TABLE entity 
ADD COLUMN user_id UUID REFERENCES users(id) ON DELETE CASCADE;

-- 인덱스 추가
CREATE INDEX idx_entity_user_id ON entity(user_id);
```

**영향**:
- 이 수정 없이는 모든 사용자가 모든 entity를 볼 수 있음
- RLS 정책 적용 불가
- 보안 이슈

### 2. RLS 정책 적용
모든 테이블에 RLS 정책 필요 (상세 내용은 PRD 11.1 섹션 참조)

---

## 구현 진행 현황 요약

### ✅ 완료 (약 40%)
- 프로젝트 인프라 (기술 스택, Supabase, Provider)
- 인증 시스템 (OAuth, Email/Password, LoginModal)
- React Query 기반 Generic 훅
- 기본 UI 레이아웃 및 컴포넌트 구조
- Header (로그아웃 기능 포함)
- 디자인 시스템 (색상, 스크롤바)

### ⚠️ 진행 중 (약 10%)
- 데이터베이스 테이블 (user_id 누락)
- UI 컴포넌트 (placeholder만)

### ❌ 미구현 (약 50%)
- **핵심 비즈니스 로직** (메모 저장, Entity 자동완성)
- 도메인 특화 React Query 훅
- Entity 드롭다운
- 메모 목록 표시
- Entity 하이라이트
- Toast 시스템
- RLS 정책
- 인덱스

---

**다음 단계 추천 순서**:
1. 🔴 **긴급**: entity 테이블에 user_id 추가
2. 🔴 **긴급**: RLS 정책 적용
3. 🟠 Toast 시스템 구현
4. 🟠 도메인 특화 React Query 훅 작성
5. 🟠 메모 목록 표시 (MainContainer)
6. 🟠 메모 저장 기능 (InputArea)
7. 🟠 Entity 자동완성 드롭다운
8. 🟡 Entity 하이라이트
9. 🟡 인덱스 추가
